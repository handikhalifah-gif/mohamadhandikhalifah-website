# Hostinger API token used by the Hostinger MCP servers configured in .mcp.json
# Get yours from the Hostinger hPanel API tokens page, then copy this file to .env
# (or export it in your shell) and fill in the real value. Never commit the real token.
HOSTINGER_API_TOKEN=
